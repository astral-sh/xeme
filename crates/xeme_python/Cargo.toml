[package]
name = "xeme_python"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Python bindings for the Xeme streaming XML parser"
publish = false

[lib]
name = "_native"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29.2", features = ["abi3-py310"] }
xeme.workspace = true
xeme_storage = { path = "../xeme_storage" }

[lints]
workspace = true
