# Version tokens and converted comment/PI payloads

Pinned Expat 2.8.4 accepts nonempty ASCII version tokens containing letters, digits, dots, hyphens, and underscores, including `1.x` and `2.0`. It continues using XML 1.0 character rules. It also normalizes CR/CRLF in comment and processing-instruction callback strings after character conversion, including CR introduced by an entity character reference. Character data and CDATA preserve those reference CRs.

`oracle.json.gz` retains exact document bytes (hex), chunk sizes, all recorded callbacks, statuses, errors, byte positions, library paths and SHA-256 hashes for 42 cases. Baseline is the reviewed consumed-byte-accounting library. `initial-draft-oracle.json.gz` preserves the first probe against the independent custom-encoding draft; `oracle.json.gz` records the separate build of just this ordinary-UTF-8 change. The reference accepts five formerly rejected nonempty version spellings; an empty version and non-ASCII version character remain rejected. Corrected successful callback streams and acceptance agree in all 42 cases; position/error-prefix equivalence is not claimed.

The scripts use ctypes and the pinned Expat shared library. Run `python3 probe.py --candidate /absolute/path/liboriole_expat.so`; adjust the recorded reference/baseline library paths when reproducing elsewhere. No upstream tests are edited. Existing Rust regression expectations were corrected to the oracle result.

Validation: all 232 affected core/C-adapter tests and strict affected-package Clippy pass, including exhaustive allocator failure injection and custom allocator reentry coverage. The release library for this isolated change is frozen at `/tmp/oriole-ordinary-payload-runtime/liboriole_expat.so`. No C sanitizers were rerun for this isolated safe-core correction.

The integrated follow-up on the attribute-recycling/event-layout runtime passes all 54 parser regressions, strict core Clippy, the same 42-case oracle and a 1,518-case exact differential replay against its preceding runtime. The shared library is `e0937dc609430313da51b2d00159441321bc401681913529ee1a2a9a9d357e4c`. [integrated.tar.gz](integrated.tar.gz) retains exact source/build metadata, commands and raw observations; [integrated-files.json](integrated-files.json) verifies its members. The sole cherry-pick conflict adapted version-token validation to the existing raw-offset attribute representation.
