# Shared parameter-child DTD tables

Frozen implementation based on `ebcd46d` plus the constructor and active-index overlays recorded in `source.json`. `shared-dtd.patch` applies after those overlays; `source.tar.gz` preserves the exact tested source. Root design review is not implementation approval. Peer source review found no blocker; root implementation review is pending.

Parameter children immediately share committed entity/default definitions, including commits before nonfinal or failed child parsing. General children keep independent snapshots. Safe scoped table ownership returns maps on success, error and panic; no table reference or guard reaches a C callback. Local namespace/active indexes keep independent hash identities. Family declaration caps remain enforced when a parameter child raises local limits. Actual owner bytes use the selected allocator; fixed owner storage is not XML expansion work.

The legal 432-case publication oracle falls from 264 full differences to 9, with zero parent/child outcome differences. Remaining 9 are the recorded standalone error 24 callback-timing gap; no callback waiver or full Expat-compatibility claim. Full native upstream observations are unchanged at 4,077 passes and 663 failures, with the original assertions, retry ceilings and bounds. The matrix excludes the adapter's original private-API tests. Full custom external provenance: 15,288 cases, membership: 4,680 cases and doctype: 4,104 cases checks are retained. Six C-only ASan/UBSan executables passed; this does not instrument the Rust release and does not claim a full Rust sanitizer campaign.

Child creation stays at 5 selected mallocs from 0 through 128 inherited definitions; the former 128-definition child needed 268. The ordinary no-namespace constructor remains 6. Seven paired native process cohorts show substantial DTD gains but approximately 11% overhead for the empty tiny document in this screen. Real-project parse ratios span 0.982–1.028. These are shared-host microbenchmarks with callback preflights, not application throughput.

All failed development attempts remain present. The 100-empty-header-request limit test became cheaper after copies disappeared; its input now has 1,000 requests under the unchanged 8,192-byte work limit. Zero-indirect-work fixtures remain unchanged. The initial upstream launch missing runner.c is a setup failure, superseded by the complete 4,740-case run; it is not a compatibility observation.

Generators retain exact executed absolute paths. `dependency-paths.json` preserves external helper sources; rebase paths and provide the pinned Expat source/library to rerun. Libraries/executables are omitted; source/build commands and hashes are retained. Initial/evidence labels distinguish older draft results from release `cfe373a6`. The benchmark package records all input and tool hashes; project XML inputs live in the repository corpus.
