# Long inherited default attribute

The permanent C consumer now checks the exact 1,024-byte default attribute inherited by an external general-entity child. The original Expat allocation test and our earlier [success probe](../allocation-success/) only established successful parsing; neither asserted the delivered value. No new parser defect was found.

The fixture retains the upstream root XML and child `<e/>`. Its independent oracle checks element `e`, the sole attribute `a1`, all 64 repetitions of `ABCDEFGHIJKLMNOP`, and zero specified attributes. Inherited handlers must report exactly one parent and child start/end, one external load, and successful child completion/free before the parent ends. The custom allocator must balance after every case. Six chunk widths (0 through 5) and both deferral settings produce 12 configurations.

All 12 configurations pass against pinned Expat 2.8.4 and the selected suffix-sharing Oriole PGO library with shared and static linkage. The complete existing C integration consumer also passes in all three runs. Only the consumer and its upstream attribution notice change; the selected runtime remains `a0acaf1bf8c8ae3f55030332be794cb8b9b5bea9`, published in the [element-name storage layer](../element-name-storage/). The other 71 files in its measured source set match exactly.

The [independent review](independent-review.json) confirms the fixture, active value/lifecycle assertions and linkage for all 36 configurations. [report.json](report.json) retains commands, source/library identities and exits; adjacent logs preserve compiler, ELF, loader-resolution and consumer output. These establish compile/ELF/loader resolution; the consumer does not emit same-process `dladdr` origins. The C consumer uses ASan and UBSan. The Rust and Expat release libraries are uninstrumented, leak detection is disabled, and explicit allocation balance is asserted. These successful-content checks do not establish exhaustive allocation-failure behavior or change the original upstream API matrix. No new timing or full CPython campaign was necessary for this test-only change.

The initial controller stopped before compiling because its blanket source-equality assertion included the intentionally changed C test. The [preparation note](attempt01.md) and original controller retain that error; the corrected controller requires exactly the reviewed test-file change. Absolute paths describe the original execution environment, not a portable benchmark installation.
