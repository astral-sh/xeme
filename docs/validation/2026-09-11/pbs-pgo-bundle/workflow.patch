diff --git a/.github/workflows/pbs.yml b/.github/workflows/pbs.yml
index 08b4160..7201f6c 100644
--- a/.github/workflows/pbs.yml
+++ b/.github/workflows/pbs.yml
@@ -6,6 +6,12 @@ on:
       - '.github/workflows/pbs.yml'
       - 'integration/python-build-standalone/**'
   workflow_dispatch:
+    inputs:
+      pgo:
+        description: Train and bundle a fresh Oriole PGO library
+        type: boolean
+        required: false
+        default: false
 
 permissions:
   contents: read
@@ -20,6 +26,8 @@ jobs:
     if: github.event_name == 'workflow_dispatch' || startsWith(github.head_ref, 'charlie/codex-oriole-pbs-')
     runs-on: ${{ github.repository_owner == 'astral-sh' && 'github-ubuntu-24.04-x86_64-4' || 'ubuntu-24.04' }}
     timeout-minutes: 180
+    env:
+      ORIOLE_PGO: ${{ (github.event_name == 'workflow_dispatch' && inputs.pgo) || (github.event_name == 'pull_request' && startsWith(github.head_ref, 'charlie/codex-oriole-pbs-pgo-')) }}
     steps:
       - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
         with:
@@ -39,11 +47,24 @@ jobs:
         run: |
           set -o pipefail
           cargo build --release --locked 2>&1 | tee "$RUNNER_TEMP/pbs-validator-build.log"
+      - name: Prepare optional PGO tools
+        if: ${{ env.ORIOLE_PGO == 'true' }}
+        run: |
+          rustup component add llvm-tools-preview
+          cargo fetch --locked --target x86_64-unknown-linux-gnu
+          oriole_profdata="$(rustc --print sysroot)/lib/rustlib/x86_64-unknown-linux-gnu/bin/llvm-profdata"
+          test -x "$oriole_profdata"
+          printf 'ORIOLE_PGO_PROFDATA=%s\n' "$oriole_profdata" >> "$GITHUB_ENV"
       - name: Build the frozen Oriole bundle
         run: |
           rustup component add rust-docs
           python3 integration/python-build-standalone/validate-tls.py --output "$RUNNER_TEMP/pbs-tls-probe"
-          python3 integration/python-build-standalone/prepare.py --output "$RUNNER_TEMP/oriole-bundle"
+          if [[ "$ORIOLE_PGO" == 'true' ]]; then
+            python3 integration/python-build-standalone/prepare.py --output "$RUNNER_TEMP/oriole-bundle" \
+              --pgo --llvm-profdata "$ORIOLE_PGO_PROFDATA"
+          else
+            python3 integration/python-build-standalone/prepare.py --output "$RUNNER_TEMP/oriole-bundle"
+          fi
           git -C .cache/pbs apply --check "$GITHUB_WORKSPACE/integration/python-build-standalone/pbs-a455388.patch"
           git -C .cache/pbs apply "$GITHUB_WORKSPACE/integration/python-build-standalone/pbs-a455388.patch"
       - name: Build CPython 3.12.13 with Oriole
@@ -104,6 +125,11 @@ jobs:
             ${{ runner.temp }}/pbs-archive-sha256.txt
             ${{ runner.temp }}/oriole-bundle/manifest.json
             ${{ runner.temp }}/oriole-bundle/build.log
+            ${{ runner.temp }}/oriole-bundle/pgo/latest.json
+            ${{ runner.temp }}/oriole-bundle/pgo/runs/
+            !${{ runner.temp }}/oriole-bundle/pgo/runs/**/*.a
+            !${{ runner.temp }}/oriole-bundle/pgo/runs/**/*.so
+            !${{ runner.temp }}/oriole-bundle/pgo/runs/**/*.dylib
             ${{ runner.temp }}/pbs-tls-probe/manifest.json
             ${{ runner.temp }}/pbs-tls-probe/*-symbols.txt
           retention-days: 14
