# Raw-view supplemental buffer-tail: held

This directly reuses the completed selected-whitespace diagnostic's C/header
bytes, controls, compiler vector, reader result arithmetic and checked subreaper
execution/cleanup body. Exactly the existing two second-empty-buffer allocation-result assertions become unconstrained status/error/counter observations, with the existing stdio include. All other original tail assertions and API order remain unchanged. The12 ordered configurations retain3-second per-case,1GiB address-space,768MiB sampled-RSS and240-second total limits.

The current candidate is uncommitted on base2c4d216: exact nine-file raw-view plus
single-input-owner source, completed build-binding b2027cd8 and normal SO e59d89d6.
`runtime_commit` is null. The controller checks the actual HEAD and complete
binary Git diff against the bound patch, alongside every main74+auxiliary4 hash.
The saved reader checks those recorded commands and patch bytes again. The
larger original4740 evidence and older supplements remain immutable and separate.

One original-O1 C compile and one diagnostic run remain held on CPU3, each with
the same existing deadlines (C compile120 seconds). There is no Rust build,
instrumentation, altered allocator or widened limit. All72 staged files are
byte-identical to the selected diagnostic; only output/library source bindings
and uncommitted-runtime identity checks change. Failures, timeouts, observations
and incomplete selections remain explicit; passing supplemental outcomes do not
reclassify any original allocation-schedule result.

Root reviews the direct run.py/read.py diffs and uses root-command.json only after
releasing qualification outside elapsed timing. The saved reader then takes
--released-preparation-sha and --released-controller-report-sha on CPU6. No runner,
reader, compiler, parser, interpreter target or future output was executed/read
during preparation. Candidate binary hashes are inherited from the completed
build-binding and will be verified before/after the actual diagnostic.
